apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "istio-bookinfo.fullname" . }}-test-connection"
  labels:
    {{- include "istio-bookinfo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "istio-bookinfo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
