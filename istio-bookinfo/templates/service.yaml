apiVersion: v1
kind: Service
metadata:
  name: {{ include "istio-bookinfo.fullname" . }}
  labels:
    {{- include "istio-bookinfo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "istio-bookinfo.selectorLabels" . | nindent 4 }}
